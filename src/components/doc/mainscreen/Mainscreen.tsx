import { FC } from "react";
import { styles } from "../build/DocStyles";

const Mainscreen: FC = () => {

    return (
        <div className={ styles.main_doc }>

        </div>
    )
}

export default Mainscreen