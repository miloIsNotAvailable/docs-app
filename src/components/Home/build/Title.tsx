import { FC } from "react";
import { styles } from "./HomeStyles";

const Title: FC = () => {

    return (
        <div className={ styles.title }>
            your projects.
        </div>
    )
}

export default Title